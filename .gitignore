# runtime data — the shared SQLite database and WAL files
meados.db
*.db
*.db-wal
*.db-shm
.backups/

# macOS
.DS_Store

# python bytecode
__pycache__/
*.pyc

# local tooling
.claude/

# user-uploaded images (per-install content, not part of the repo).
# assets/icons + assets/vendor ARE tracked (bundled app assets).
assets/labels/
assets/photos/
assets/brand/
